// Generated by scripts/build-reader.mjs; edit lib/ sources.
import { contentIdFor, platformFor } from '../shared/links.mjs';
import { emptyResolution, parseLinkPage, terminalLinkRoute } from '../shared/link-page.mjs';
export const RESOLVER_VERSION = '3.4';
export { resolutionCacheTtl as resolverTtl } from '../shared/resolution-cache.mjs';
class ReadError extends Error {
    code;
    constructor(code) { super(code); this.code = code; }
}
export async function resolveLink(start, platform, fetcher = fetch) {
    let current = start, workUrl = start, upstreamStatus = 0;
    let summarySelected = false;
    let summaryRequested = false;
    const redirects = [];
    const started = Date.now(), signal = AbortSignal.timeout(18000);
    const finish = (result) => ({
        ...result, resolverVersion: RESOLVER_VERSION,
        // No share tokens, cookies, body text or signed media URLs in diagnostics/logs.
        diagnostics: { upstreamStatus, redirects, elapsedMs: Date.now() - started },
    });
    try {
        for (let hop = 0; hop < 6; hop++) {
            if (platformFor(current) !== platform)
                throw new ReadError('invalid_redirect');
            const terminal = terminalLinkRoute(platform, current);
            if (terminal)
                throw new ReadError(terminal);
            const knownId = contentIdFor(platform, workUrl), nextId = contentIdFor(platform, current);
            if (knownId && nextId && knownId !== nextId)
                throw new ReadError('identity_mismatch');
            if (!knownId && nextId)
                workUrl = current;
            // Select the public summary document before requesting a known video.
            // This is NOT a fallback after a login, CAPTCHA, rate limit or denial.
            // Notes/slides and ambiguous modal_id links keep their original route.
            if (platform === 'douyin' && !summarySelected && nextId &&
                /^(?:www\.)?(?:douyin|iesdouyin)\.com$/.test(current.hostname) &&
                /^\/(?:share\/)?video\/\d+\/?$/.test(current.pathname)) {
                current = new URL(`https://jingxuan.douyin.com/m/video/${nextId}`);
                summarySelected = true;
            }
            const isSummary = current.hostname === 'jingxuan.douyin.com';
            if (isSummary && !/^\/m\/video\/\d+\/?$/.test(current.pathname))
                throw new ReadError('unsupported_page');
            if (isSummary)
                summarySelected = true;
            // Do not turn a redirect off the summary page into a second access method.
            if (summarySelected && !isSummary)
                throw new ReadError('unsupported_page');
            if (isSummary && summaryRequested)
                throw new ReadError('invalid_redirect');
            if (isSummary)
                summaryRequested = true;
            const response = await fetcher(current, {
                redirect: 'manual', signal,
                headers: {
                    // Request the public desktop document directly; mobile pages commonly
                    // return App-only shells. No alternate request after an access denial.
                    'User-Agent': isSummary
                        ? 'Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1'
                        : 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36',
                    'Accept': 'text/html,application/xhtml+xml', 'Accept-Language': 'zh-CN,zh;q=0.9',
                    'Referer': platform === 'xiaohongshu' ? 'https://www.xiaohongshu.com/' : 'https://www.douyin.com/',
                },
            });
            upstreamStatus = response.status;
            redirects.push({ host: current.hostname, path: current.pathname.slice(0, 160), status: response.status });
            if ([301, 302, 303, 307, 308].includes(response.status)) {
                const location = response.headers.get('location');
                await response.body?.cancel();
                if (!location)
                    throw new ReadError('invalid_redirect');
                try {
                    current = new URL(location, current);
                }
                catch {
                    throw new ReadError('invalid_redirect');
                }
                continue;
            }
            if (response.status === 429)
                throw new ReadError('rate_limited');
            if (response.status === 401)
                throw new ReadError('login_required');
            if (!(response.headers.get('content-type') ?? '').toLowerCase().includes('text/html')) {
                await response.body?.cancel();
                throw new ReadError(response.status === 403 ? 'access_denied' : response.status === 404 ? 'not_found' : response.status >= 500 ? 'network_error' : 'unsupported_page');
            }
            const reader = response.body?.getReader(), decoder = new TextDecoder();
            let html = '', size = 0;
            if (reader)
                while (true) {
                    const chunk = await reader.read();
                    if (chunk.done)
                        break;
                    size += chunk.value.length;
                    if (size > 2_000_000) {
                        await reader.cancel();
                        throw new ReadError('unsupported_page');
                    }
                    html += decoder.decode(chunk.value, { stream: true });
                }
            html += decoder.decode();
            return finish(parseLinkPage(html, platform, workUrl, current, response.status));
        }
        throw new ReadError('invalid_redirect');
    }
    catch (error) {
        const reason = error instanceof ReadError ? error.code : signal.aborted || (error instanceof Error && /TimeoutError|AbortError/.test(error.name)) ? 'timeout' : 'network_error';
        return finish(emptyResolution(platform, workUrl, reason));
    }
}
