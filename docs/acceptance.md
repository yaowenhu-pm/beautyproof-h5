# V2交付验收记录

日期：2026-09-06。此为小样本验收，不是准确率评测。

## 已完成免费检查

- `npm run test:v2`：25项通过。包括来源id/原文校验、伪造引文拒绝、30成分/别名/盐和酯区分、链接主机与签名参数、XHS与抖音页面状态结构、持久化预算SQL、重复预留/测试上限/不明费用保留。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `npm run test:rules`：6条历史规则用例通过；这是旧算法回归，不是V4准确率证明。旧规则不再决定新版报告的最终结论。
- 本地真实API与浏览器：未配置本地付费开关时，返回“AI分析尚未启用”，保留甘油识别和资料，不冒充模型分析成功。
- 官方 `/models`：密钥可用，包含 `deepseek-v4-flash`；此验证未调用生成接口。

## 尚待本轮线上验证

受预算控制的4—6次真实分析、截图OCR、两平台链接可读性、手机报告与公开访问。最终记录将在完成后更新；此处不预填成功结果。

## 费用口径

所有本轮生成请求只在生产网站发起，使用同一个D1账本。本地不配置真实付费密钥，避免分散预算。
人民币微元记账：总上限1,000,000微元（1元），管理员测试上限600,000微元（0.6元）。峰值价格3元/百万输入token、9元/百万输出token；缓存折扣不用于增加余额。无usage的超时保留全部预留额。
已收到usage的记录按峰值价格保守记账，实际账单可更低；不能把这个保守账本称为官方实际扣费。未收到usage的费用不明，不能视作免费。
价格核验来源：[DeepSeek官方价格页](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)，2026-09-06核阅。价格有效期后自动暂停新的付费请求，需复核价格再更新；当前到2026-09-09 00:00北京时间。
