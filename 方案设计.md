# 真妍盾 BeautyProof：赛道二 H5 Demo 方案

> 2026-08-24 实现状态：H5 已接入真实的平台链接解析、浏览器端媒体特征提取、D1 内容指纹查重库、C2PA/EXIF 读取和宣称规则核验。深度伪造分类、平台完整媒体抓取及法规 RAG 仍属于下一阶段。

## 1. 产品定义

真妍盾不是一个“AI 图片二分类器”，而是一套面向美妆内容的证据型鉴真 Agent。它接收小红书/抖音公开作品链接、用户文字、图片或视频，从四条证据线回答：内容来自哪里、媒体是否被篡改、功效宣称是否有依据、用户接下来应该做什么。

核心原则是“证据不足不等于虚假”。系统允许输出待核验、不确定和需要人工复核，避免用单一模型分数替代事实判断。

## 2. 本次 H5 Demo

- 链接入口：识别小红书与抖音作品链接，也能从分享口令中提取 URL。
- 本地素材：拖拽或选择图片、视频、文本文件，单文件上限 200MB，最多 8 个。
- 文字核验：输入种草文案、商品宣称或评论区话术，自动进入声明拆解流程。
- 分阶段分析：来源解析、声明提取、视觉取证、专业证据检索、报告生成。
- 可解释报告：综合风险、四维评分、逐条证据、视觉热力图、引用来源与行动建议。
- 创作者保护：演示原创登记、内容指纹、篡改比对与证据包导出的完整故事线。

当前版本是交互式产品样机，分析结果使用稳定的示例证据驱动，便于比赛演示；链接抓取、模型推理和法规知识库应部署在服务端后再替换为真实接口。

## 3. 推荐技术架构

```text
H5 / 管理台
   │
   ▼
Input Adapter ── 小红书适配器 / 抖音适配器 / 文件上传 / 纯文本
   │
   ▼
内容标准化 ── 元数据、关键帧、OCR、ASR、原子声明
   │
   ├── 来源证据：C2PA、EXIF、SHA-256、pHash、发布时间线
   ├── 媒体取证：生成检测、局部篡改定位、人脸精修、帧间一致性
   ├── 宣称核验：NMPA 法规/备案/评价摘要 RAG、成分逻辑、引用质量
   └── 传播证据：跨平台近似匹配、最早发布、版本差异
   │
   ▼
Evidence Graph ── 每条结论绑定来源、模型版本、置信度和原始片段
   │
   ▼
Decision Agent ── 确定性规则 + 校准分数 + 弃权/人工复核
   │
   ▼
HTML / PDF / 申诉证据包
```

### 输入与解析

浏览器不直接抓取小红书或抖音页面。生产版由服务端 `Input Adapter` 接收公开链接，执行域名白名单、重定向解析、平台适配、限流和缓存。平台页面结构、Cookie、签名和反爬策略都可能变化，所以适配器必须可热更新，并遵守平台规则与用户授权边界。

上传文件先进入对象存储，使用短期签名 URL；元数据解析、转码与抽帧放到异步任务中。默认检测完成后 24 小时删除原始文件，报告只保留经用户授权的摘要和哈希。

### 来源与视觉取证

- `c2pa-rs`：验证 Content Credentials 的签名、声明与编辑历史；缺少凭证只能得出“不可验证”，不能推出“伪造”。
- SHA-256 + pHash：前者确认字节级一致，后者匹配裁剪、压缩、轻微调色后的近似内容。
- 多模型组合：全图生成检测、局部篡改定位、人脸精修与视频帧间一致性分别出信号，不使用单模型一票否决。
- 输出热力图时同时保存模型版本、阈值和不确定性；遇到压缩严重、分辨率过低或模型冲突时自动转人工复核。

### 宣称核验

1. PaddleOCR/ASR 提取画面、标题和口播。
2. 声明拆分器把“7 天焕白一个色号”拆为时间、效果、量化幅度、对象与条件。
3. 先检索权威法规和产品备案/功效宣称评价摘要，再检索品牌提供的产品级证据。
4. 每条结论必须引用可定位的证据片段；仅凭原料论文不能直接推出成品功效。

### Agent 裁决

大模型负责组织证据与解释，不直接自由打分。风险分由可审计状态机计算：信号先做置信度校准，再按证据独立性、来源等级和任务类型融合。规则保留三态输出：支持、反驳、证据不足；涉及法律定性或高影响处置时必须人工确认。

## 4. 可复用的开源组件

| 模块 | 候选项目 | 采用方式 | 许可/风险提示 |
|---|---|---|---|
| 内容凭证 | `contentauth/c2pa-rs` | 生产候选 | MIT / Apache-2.0 |
| OCR | `PaddlePaddle/PaddleOCR` | 生产候选 | Apache-2.0 |
| 近似图像检索 | `idealo/imagededup` | 快速基线；规模化时迁移向量索引 | Apache-2.0 |
| 小红书链接适配 | `JoeanAmier/XHS-Downloader` | 参考其 HTTP API 和解析层，不把抓取逻辑耦合到 H5 | MIT；需遵守平台规则 |
| 抖音链接适配 | `Evil0ctal/Douyin_TikTok_Download_API` / `yt-dlp` | 只借鉴适配器隔离、Cookie 和失败回退机制 | 上线前逐项复核许可与平台条款 |
| 图像篡改研究 | `grip-unina/TruFor` | 仅研究评测，不进入商业生产 | 仓库限制为信息与非营利用 |
| 多线索鉴伪 | `mi-lad/CO-SPY` | 借鉴语义/伪影双分支与可解释输出 | 采用代码前必须确认许可证 |

开源模型只能解决证据链中的部分问题。比赛 Demo 可以组合成熟基线，商业化版本需要自建美妆场景数据、校准阈值，并确保每个组件的许可允许目标用途。

## 5. 后端接口草案

```http
POST /api/v1/analyses
Content-Type: application/json

{
  "sourceType": "xiaohongshu | douyin | upload | text",
  "url": "https://...",
  "assetIds": ["asset_..."],
  "text": "连续使用7天……",
  "consent": true
}
```

返回 `analysisId` 和任务状态。前端通过 SSE 或轮询获取阶段进度，最终报告至少包含：`sourceEvidence`、`mediaFindings`、`claimFindings`、`citations`、`riskLevel`、`confidence`、`abstained`、`recommendedActions` 和完整审计日志。

## 6. Demo 到可用产品的迭代

### 72 小时比赛版

- 接好一个真实的链接解析适配器和文件上传接口。
- 跑通 PaddleOCR、关键帧抽取、pHash、C2PA 验证。
- 准备 6–10 个可控案例：真实原创、搬运裁剪、局部精修、AI 生成、夸大宣称、证据充分。
- 把真实流水线结果转换为当前报告 JSON，保证前端无需重写。

### 两周增强版

- 接入本地视觉取证模型，完成阈值校准和失败检测。
- 建立 NMPA 法规、产品备案和功效评价摘要知识库。
- 加入证据图、报告版本与人工复核台。
- 完成隐私删除、审计日志、限流和域名白名单。

### 评测指标

- 视觉取证：AUC/F1 之外，重点看校准误差、热力图定位 IoU 和低质素材拒答率。
- 宣称核验：原子声明召回率、证据引用准确率、结论可支持率、无依据断言率。
- 端到端：用户能否在 30 秒内理解风险、找到依据并完成下一步行动。
- 安全性：真实内容误伤率、创作者申诉成功率、隐私删除成功率。

## 7. 演示脚本

1. 粘贴一条含“7 天焕白一个色号”的小红书或抖音分享链接。
2. 展示五阶段分析，强调它不是黑箱秒判。
3. 在报告中依次打开关键结论、全部证据与参考来源。
4. 指出“视觉可疑”和“宣称证据不足”来自不同证据线，结论仍保留不确定性。
5. 切到创作者保护，说明同一套来源与指纹能力既能打假，也能保护真实创作。
